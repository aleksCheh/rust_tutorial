use crate::dungeon_crawler::prelude::*;

const NUM_ROOMS: usize = 20;

pub struct CrawlerMapBuilder {
    pub map: CrawlerMap, 
    pub rooms: Vec<Rect>,
    pub player_start: Point,
}
impl CrawlwerMapBuild {
    
}